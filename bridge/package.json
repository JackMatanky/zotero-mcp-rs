{
  "name": "zotero-companion-bridge",
  "private": true,
  "version": "1.0.0",
  "description": "Companion Zotero script bridging Zotero in-process APIs to the Zotero MCP server's loopback HTTP interface.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "zotero-types": "^4.1.3"
  }
}
